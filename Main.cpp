#include "Graph.h"

/*
This Program gets a graph as a input, and returns the shortest paths for a single source to another
destination. 
In this program we use couple algorithms that might help us to get the desired output.
*/

int main()
{
	Graph G = *(Graph::Init());
	G.RunTime();
}